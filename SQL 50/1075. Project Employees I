# Write your MySQL query statement below
SELECT project_id,ROUND(AVG(Employee.experience_years),2) as average_years  FROM Project JOIN Employee on Project.employee_id =Employee.employee_id GROUP BY Project.project_id;
